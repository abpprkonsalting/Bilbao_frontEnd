import { ContractProgressPipe } from './contract-progress.pipe';

describe('ContractProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
