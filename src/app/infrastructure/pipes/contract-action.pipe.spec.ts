import { ContractActionPipe } from './contract-action.pipe';

describe('ContractActionPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractActionPipe();
    expect(pipe).toBeTruthy();
  });
});
