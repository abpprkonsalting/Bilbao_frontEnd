import { OfferActionPipe } from './offer-action.pipe';

describe('OfferActionPipe', () => {
  it('create an instance', () => {
    const pipe = new OfferActionPipe();
    expect(pipe).toBeTruthy();
  });
});
